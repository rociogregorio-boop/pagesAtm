console.log("hola")
// este es un comentario
/*var nombre;
let apellido;
const PAIS = "Peru"
console.log(nombre)
var nombre = "Diego"
 console.log("2" === 2)
 prompt -- recibe mens */
 /*alert("hola alert desde")*--- mensaje unidireccional*/
 /*et usuario = "Diego"
 alert("hola" + usuario)
 let edad = "30"
 let peso = 70 
 let altura=1.75

 let indiceDeMasaCorporal = peso / (altura**2)

 alert("Hola usuario, tu indice de masa corporal es:  " + indiceDeMasaCorporal)
 */
 /*const verificarNum = (num) => {
     return new Promise (( resolve, reject) => {
        if (typeof num === 'number') {

        }else{
        }

        });
    }
    verificarNum(42)
      .then(resultado => {
        console.log(resultado);
      })
      .catch(error =>{
        console.error(error);
      });*/
/*let usuario = prompt("hola usuario, imgrese tu nombre:")
  console.log(nombre) }
}
                        */
